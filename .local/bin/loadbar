#!/usr/bin/env bash
MONITOR_LIST=(
  $(xrandr --listmonitors | awk '{print $4}' | tail -n +2 | sort -r)
)

for i in ${MONITOR_LIST[@]}; do
  if [[ $i == "DisplayPort-1" ]]; then
    polybar -r owari-primary &
  else
    MONITOR=$i polybar -r owari-generic &
  fi
  sleep 1;
done

